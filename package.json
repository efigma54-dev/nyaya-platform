{
  "name": "nyaya-platform",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "docker compose up --build",
    "dev:frontend": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "lint": "npm --prefix frontend run lint",
    "evidence": "python -m nyaya.cli validate --evidence-dir ./evidence"
  }
}
